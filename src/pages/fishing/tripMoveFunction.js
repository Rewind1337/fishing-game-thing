import GLOBALS from '../../globals/Globals';

let tripMoveFunction = function (location, subLocation) {
  let event = {id: 0, name:'Nothing Happened'};
  return event;
};

export default tripMoveFunction;